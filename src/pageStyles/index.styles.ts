import { CSSProperties } from 'react'

export const indexStyles: { [key: string]: CSSProperties } = {
  wrapper: {
    position: 'relative',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    height: '100vh',
  },
  filterInput: { marginBottom: 15 },
}
